"""Memory as Code runtime package.

This file makes the local ``code`` directory a regular Python package so
``python -m code`` resolves to this project rather than Python's standard
library module of the same name.
"""
